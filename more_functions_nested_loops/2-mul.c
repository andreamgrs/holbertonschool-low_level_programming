#include "main.h"
/**
 * mul -  multiplies two integers and returns the result.
 *
 * @a: the integer to evaluate.
 * @b: the integer to evaluate.
 *
 * Return: the multiplication of the 2 integers.
 */
int mul(int a, int b)
{
	return (a * b);
}
