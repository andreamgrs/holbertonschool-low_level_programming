#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 *
 * @a: the integer to evaluate.
 * @b: the integer to evaluate.
 *
 * Return: the result of the 2 integers.
 */
int add(int a, int b)
{
	return (a + b);
}
